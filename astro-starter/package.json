{
  "name": "sistecpos-astro-tenant",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist --compatibility-flag=nodejs_compat",
    "deploy": "wrangler pages deploy ./dist",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.5.0",
    "@astrojs/tailwind": "^6.0.0",
    "astro": "^5.8.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
